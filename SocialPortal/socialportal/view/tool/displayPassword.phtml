<?php
if( !isset( $vars['encoded'] ) ) {
	echo "Usage: you need to pass arguments to the url like /tool/action/UserKey/password in order to have results";
} else {
	echo "Password: [" . $vars['encoded'] . "]";
}