<?php
if( !isset( $vars['encoded'] ) ) {
	?><div class="message info centered"><?php
	echo __( 'Usage: you need to pass arguments to the url like /tool/directCreatePassword?key=xxx&password=yyy in order to have results' );
	?></div><?php
} else {
	echo "Password: [" . $vars['encoded'] . "]";
}